const PageNotFound = () => {
  return (
    <div>
      Esta es la página que no existe. Por favor, verifica la URL o vuelve a la
      página principal.
    </div>
  );
};

export default PageNotFound;
